# 终端小说阅读器

## 项目概述

使用 **Node.js + Ink（React 的终端渲染库）** 开发一个 **全屏终端小说阅读器**，风格类似 Vim，以键盘为中心操作，支持快捷键呼出命令面板command panel。

---

## 核心功能需求

### 1. 文件导入
- 支持用户通过 **命令行参数** 传入 `.txt` 文件路径
- 内置一个简洁的 **文件浏览器**（文件选择器），支持浏览目录并选择 `.txt` 文件打开，可以使用yazi库
- 支持 **拖拽文件到终端窗口**（部分终端支持）作为入口之一
- 检测文件编码（UTF-8 / GBK），自动解码

### 2. 全屏阅读界面
- 启动即进入全屏（`<stdin>` fullscreen 模式或 `ink` 的 `<FullScreen>` 组件）
- 自适应终端宽度高度（`process.stdout.columns` / `rows`），窗口 resize 时实时重排
- 文本自动换行，不破坏段落结构
- 上下左右滚动阅读

### 3. Vim 风格键盘操作

| 按键 | 功能 |
|------|------|
| `j` / `↓` | 向下滚动一行 |
| `k` / `↑` | 向上滚动一行 |
| `Ctrl+d` / `PgDn` | 向下翻半屏 |
| `Ctrl+u` / `PgUp` | 向上翻半屏 |
| `g` | 跳转到文件开头 |
| `G` (Shift+g) | 跳转到文件末尾 |
| `:N` | 跳转到第 N 行 |
| `n` | 查找下一个 |
| `N` | 查找上一个 |
| `/` | 进入搜索模式 |
| `Esc` | 退出当前模式 / 关闭面板 |

### 4. 命令面板（Command Panel）
- 按 `:` 或 `Ctrl+p` 呼出命令面板
- 面板出现在底部（Vim 风格的命令行位置）
- 支持命令：
  - `:open <path>` — 打开文件
  - `:q` / `:quit` — 退出
  - `:N` — 跳转到第 N 行
  - `:/<pattern>` — 搜索
  - `:help` — 显示帮助
  - `:theme <name>` — 切换主题
  - `:encoding <name>` — 设置编码
- Tab 补全命令

### 5. 阅读体验增强
- **阅读进度条** — 底部状态栏显示阅读进度百分比、当前行号、总行数
- **书签功能** — `m` 添加书签，`` ` `` 跳转到书签
- **行号显示** — 可切换（默认关闭，`:set number` 开启）
- **标记高亮** — 搜索关键词高亮显示
- **记忆阅读位置** — 退出时记住上次位置，下次打开自动跳转
- 底部状态栏— 提示部分快捷键，比如命令面板快捷键等

### 6. 多主题支持
- 内置 2-3 个主题（深色护眼 / 浅色 / 高对比度）
- 纯文字阅读主题，前景色 + 背景色 + 高亮色足够
- 可通过 `:theme` 切换

### 7. 键盘映射灵活性（加分项）
- 用户可自定义按键绑定（通过 JSON 配置文件 `~/.novel-reader/keymap.json`）
- 默认配置即上述 Vim 风格

### 8. CLI 用法

```bash
# 直接打开
novel ./path/to/book.txt

# 从上次位置继续
novel --resume

# 指定编码
novel ./book.txt --encoding gbk

# 不打开文件，启动文件浏览器
novel --browse

# 或者不指定选项, 打开默认界面展示用户书架
novel
```

---

## 约束

- 仅依赖 Ink 及其生态（`ink-text-input`、`ink-box` 等），不使用 `blessed` 或 `neo-blessed`
